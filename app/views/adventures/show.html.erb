<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-9">
    <%= image_tag(@adventure.picture_url, width: '100%') if @adventure.picture.present? %>
  </div>

      <div class="col-md-3">
    <p><b>Name: </b><%= @adventure.name %></p>
    <p><b>Description: </b><%= @adventure.description %></p>
    <p>
      <%= link_to 'Make a change', edit_adventure_path(@adventure) %> |
      <%= link_to 'Delete 4eva', @adventure, data: { confirm: 'Are you sure?' }, method: :delete %> |
      <%= link_to 'Baby got back', adventures_path %>
    </p>
  </div>
</div>